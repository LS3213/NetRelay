# 未来开发阶段与里程碑规划

[返回索引](README.md)

为了稳步推进 NetRelay 后续规划功能的落地，现将所有待开发功能划分为四个演进阶段。每个阶段包含明确的目标、核心特性设计和验证标准，确保程序在稳定性、功能性与易用性上逐步增强。

---

## 阶段划分总览

| 阶段 | 代号 | 核心目标 | 包含特性 |
| --- | --- | --- | --- |
| **第五阶段** | **稳健运行 (Robustness)** | 增强异常容错、确保无残留干净退出、实现日志自动回收 | 1. COM API 异常隔离机制<br>2. 异步任务优雅取消 (CancellationToken)<br>3. 日志自动滚动与容量清理 |
| **第六阶段** | **环境感知 (Detection)** | 提升网络状态判定精准度、增加 WiFi 环境自适应能力 | 1. ICMP Ping 辅助探测<br>2. DNS 解析连通性验证与域名请求<br>3. Windows NLM/NCSI 状态读取<br>4. 基于 WiFi SSID 的规则过滤 |
| **第七阶段** | **富交互 (Interaction)** | 提升前台参数可视配置度、实现 Toast 直接快速交互 | 1. 规则防抖/冷却时间 UI 可视化设置<br>2. 日志保留参数可视化设置<br>3. 可交互 Windows Rich Toast 通知 |
| **第八阶段** | **交付打包 (Delivery)** | 完成产品最后打包部署、自启规范化与真机最终验收 | 1. 引导式安装包 (WiX/Inno Setup)<br>2. .NET 8 桌面运行时自动依赖检测<br>3. 完整安装/卸载逻辑与自启动注册表校验 |

---

## 详细开发计划

### 1. 第五阶段：稳健运行 (Robustness)

#### 目标
聚焦于程序的健壮性，确保程序在面对复杂的 Windows COM API 错误时不会意外闪退，并且在用户选择“关闭”时，能够 100% 干净、顺时且无进程残留地完全退出，同时解决日志无限堆积的存储隐患。

#### 核心任务
1. **Windows COM API 异常隔离 (Try-Catch 强化)**
   - **设计**：在 `NativeNetworkConnectionService` 中对所有 COM 属性读取及网卡启用/禁用接口调用实施精细的 Catch 保护。如果由于系统 COM 服务繁忙返回异常，向 UI 报告失败信息并触发重试，避免直接向上抛出导致主线程崩溃。
2. **异步任务优雅取消 (CancellationToken)**
   - **设计**：在 `MainViewModel` 和 `RuleSchedulerService` 拥有的所有后台 Task（如每 5 秒的 HTTP 联网探测、流量采样等）中注入 `CancellationToken`。
   - **退出拦截**：在 `MainWindow_Closing` 事件触发时，首先触发 `Cancel()`，确保所有后台网络探测线程在毫秒级内感知并安全退出，之后立刻关闭主进程，保证“关闭即完全终结”。
3. **日志自动滚动与清理 (Log Rotation)**
   - **设计**：在 `LogService` 中新增容量清理方法。
   - **逻辑**：在程序启动时，异步扫描 `%LOCALAPPDATA%\NetRelay\logs\` 下的所有日志文件，对于超过保留天数（如 30 天）或累积大小超过设定上限的文件执行安全物理删除。

#### 验收标准
- 使用模拟错误注入测试 COM 接口异常，程序应正常运行不崩溃，并在界面提示诊断信息。
- 用户点击关闭窗口退出程序后，任务管理器中 `NetRelay.exe` 进程在 1 秒内完全消失，无残留线程。
- 手动生成 30 个历史日志文件，启动程序后，超出期限的文件能自动被清理。

---

### 2. 第六阶段：环境感知 (Detection)

#### 目标
解决在校园网、企业代理或公共 Wi-Fi 重定向环境下单一 HTTP 探测可能失效（如重定向导致虚报联网）的问题，并为自动化规则引入 WiFi 场景特征感知。

#### 核心任务
1. **多协议网络探测 (ICMP Ping / DNS 解析)**
   - **设计**：重构 `ConnectivityService`。在 HTTP 探测的基础上，并发进行 ICMP Ping（如 Ping `223.5.5.5`）以及轻量 DNS 域名解析（如解析公共域名）。
   - **后端辅助逻辑**：在后续与后端打通时，通过向指定的后端 API 地址发送解析与握手请求，检验真实外网连通性。
2. **Windows 原生 NLM/NCSI 状态监听**
   - **设计**：通过 P/Invoke 或 .NET Native 接口，读取 Windows 的 Network List Manager，获取系统对当前活动连接的官方联网状态评估，作为辅助的决策因子。
3. **基于 WiFi SSID / 网关 MAC 的规则过滤器**
   - **设计**：在规则配置实体类 `AutomationRule` 中新增可选属性 `TargetSsid` 和 `TargetGatewayMac`。
   - **调度执行**：调度器在满足时间触发条件时，首先检查当前连接的无线网络 SSID。如果不匹配配置的 WiFi，则直接跳过执行，记为“非目标 WiFi 环境，已忽略”。

#### 验收标准
- 在拦截 HTTP 的断网环境下，Ping 与 DNS 检测能准确判断真实断网。
- WiFi 规则能根据物理连接的变化，智能在“宿舍 SSID”下生效规则，而在“图书馆/移动热点 SSID”下挂起规则。

---

### 3. 第七阶段：富交互 (Interaction)

#### 目标
将关键调度参数和日志清理选项从文本配置文件中解放出来，提供图形化配置，并通过交互式通知提供极简的用户体验。

#### 核心任务
1. **防抖、冷却与日志清理参数 GUI 化**
   - **设计**：在 Settings 窗口中新增配置项输入滑块：
     - 网络变化判定防抖时间（秒）
     - 规则执行冷却时间（分钟）
     - 日志保留天数上限
   - **双向绑定**：将参数与 `AppConfiguration` 及 `SettingsDialog` 绑定，保存时同步重载调度器。
2. **可交互式 Windows Rich Toast 通知**
   - **设计**：利用 Windows 10/11 的 App Notification API，替代传统的托盘气泡通知。
   - **交互设计**：当规则倒计时预警发生时，屏幕右下角弹出的 Toast 内部包含实体的 **“延迟 10 分钟”** 和 **“取消本次”** 两个操作按钮。点击按钮后，系统通过进程内回调或命令行隐式唤醒通知，对主程序中的 `RuleSchedulerService` 执行相应操作。

#### 验收标准
- 设置界面配置的新参数能即时生效，且修改后在 `config.json` 中被持久化保存。
- 弹出 Toast 通知后，在不打开 NetRelay 主界面的情况下，点击 Toast 中的“延迟”按钮，能让主程序中的倒计时倒回对应分钟数。

---

### 4. 第八阶段：交付打包 (Delivery)

#### 目标
完成面向大众用户的最后一步，消除绿色版在普通系统运行时的权限、配置与依赖障碍，提供零门槛的一键安装。

#### 核心任务
1. **引导式安装包制作 (WiX/Inno Setup)**
   - **设计**：编写 Inno Setup 或 WiX 项目脚本，将 Release 交付目录下的发布文件编译打包为单个安装文件 `NetRelaySetup.exe`。
2. **.NET 8 Runtime 环境自动检测与引导**
   - **设计**：在安装程序运行之初，扫描系统注册表以检查是否已安装了相应的 `.NET 8 Desktop Runtime`。如果未检测到，安装程序会自动联网下载或引导用户访问微软官网，并静默运行运行时安装包。
3. **一键卸载与开机自启任务清理**
   - **设计**：提供标准的添加/删除程序卸载支持。在卸载时，自动调用 Windows 任务计划程序 API，彻底清理安装时或程序内部创建的 `NetRelay AutoStart` 任务项及对应的注册表项，不留任何系统垃圾。

#### 验收标准
- 在一台全新未安装 .NET 的 Windows 10/11 干净虚拟机上运行安装包，能成功自动安装依赖并使程序正常运行。
- 在控制面板卸载程序后，应用安装文件夹、注册表项、日志及计划任务全部被彻底清除。
