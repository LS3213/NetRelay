# 验收状态与实机测试清单

> **历史文档提示（2026-07-30）**：本文记录旧 NetRelay Server 的阶段验收，不再定义当前 Omnexa 接入语义。当前以 [NetRelay 接入 Omnexa](21-omnexa-client-integration.md) 为准：后台只管理设备；历史协议字段 `installationId` 现在表示按程序目录持久化的内部运行槽位，同目录升级复用、不同目录隔离，槽位不展示也不作为封禁目标。

[上一篇：安全、测试与风险](07-security-testing-and-risks.md) | [返回索引](README.md)

## 1. 当前结论

截至 2026-06-15，NetRelay 客户端及后端平台 B0-B7 阶段（包括双源更新、反馈、日志上传、公告、封锁受限模式、正式部署与反馈强化）的代码实现与阶段验收已全部完成，自动化质量门槛已全部通过。B7 反馈强化与设备指纹库扩展已从中断半成品完成修复，并由维护者确认管理后台交互、WPF 弹窗视觉表现、真实 MySQL 迁移和生产包升级均正常。由于当前本地环境未运行 Docker，B6 阶段的容器服务运行已通过严格的静态配置审查与脚本加固验证。Windows Toast 真实点击、物理网卡切换、休眠、重新登录和跨 Windows 版本行为尚未手动实机验收，不得视为全部通过。

仍未执行的项目均会改变真实网络状态或依赖实际休眠、重新登录环境，必须在用户明确授权或专用测试机上完成。

## 2. 已自动验证

| 项目 | 结果 |
| --- | --- |
| Release 解决方案构建 | 通过，0 错误；B7 接手检查中测试项目仍有 32 个 nullable/重复 using 警告，未阻塞运行测试 |
| 客户端 Release 自包含开发预览 | 通过，统一输出到 `artifacts/publish/win-x64/` |
| 仓库回归测试 | 44 项通过（含反馈日志打包与脱敏、SafeMarkdownParser 安全过滤、客户端受限模式以及策略签名校验测试） |
| 服务端集成测试 | 25 项通过（含设备激活、更新分发、更新包 GET/HEAD 探测、撤回后拒绝同版本覆盖、反馈列表/下载、公告签名校验以及策略评估与封锁控制） |
| Markdown 相对链接 | 通过 |
| Git 差异空白检查 | 通过 |
| 探测配置校验与自动化暂停 | 通过 |
| 自动恢复绕过冷却、附加条件和探测配置门禁 | 通过 |
| 定时规则重启去重 | 通过 |
| 单实例唤醒 | 通过 |
| 原生网卡状态映射与短暂漏项保留 | 通过 |
| GUID 不同字符串格式合并 | 通过 |
| 调度器 Timer 防重入 | 通过 |
| 开机自启任务路径与参数漂移校验 | 通过 |
| 自动恢复机制重构时间验证测试 | 通过 |
| 重启后已禁用网卡显示校正 | 通过 |
| 编辑规则大小写/格式 GUID 自适应匹配 | 通过 |
| COM 异常隔离与安全捕获 | 通过 |
| 异步任务优雅取消 (CancellationToken) | 通过 |
| 幂等性调度器防重复停止保护 | 通过 |
| 自定义日志保留期限、保存后即时清理与持久化 | 通过 |
| 非法日志保留期限安全回退到默认 30 天 | 通过 |
| 探测配置放宽校验支持 PING/DNS | 通过 |
| DNS 编解码与 Mock 解析响应测试 | 通过 |
| NLM COM 声明、构建与辅助状态接入 | 通过；跨 Windows 版本行为待真机确认 |
| 绑定网卡的 DNS UDP 探测与严格响应验证 | 通过 |
| 绑定网卡命令行 ping.exe 退出码判定 | 通过 |
| 第七阶段核心调度参数 Slider 绑定与持久化 | 通过 |
| 新建规则继承默认防抖/冷却值且已有规则不被修改 | 通过 |
| 非法设置暂停自动化且不写入配置 | 通过 |
| Rich Toast XML、权限按钮与自定义延迟逻辑 | 通过；Windows 通知中心真实点击待手动验收 |
| netrelay:// 一次性票据、随机令牌、过期与重复操作校验 | 通过 |
| 单实例命名管道 (Named Pipe) IPC 多参数数据分发 | 通过 |

## 3. 已受控实机验证

已使用不承载当前互联网流量的 `VMware Network Adapter VMnet1` 执行管理员权限受控切换：

- 禁用请求成功。
- Windows 原生 COM 在禁用后将该驱动报告为 `Disconnected`；NetRelay 根据已确认操作将其稳定映射为 `HardwareDisabled / Enabled=False`。
- 禁用期间网卡继续保留在主界面使用的合并列表中，并保持可控制。
- 重新启用请求成功，最终恢复为 `Connected / Enabled=True`。
- `RuleEngine` 的 `Schedule` 禁用与 `Recovery` 恢复路径均返回成功，恢复不受原规则冷却时间阻止。
- 使用临时配置创建的一次性规则已由真实 `RuleSchedulerService` Timer 触发，并在执行事件后成功恢复。
- 测试程序使用 `finally` 保证尝试恢复，最终退出码为 `0`。
- 2026-06-12 第五阶段重新发布后再次执行完整 VMnet1 验收，退出码仍为 `0`。

## 4. 已只读实机验证

使用第五阶段重新生成的发布版 `NetRelay.exe --diagnose-adapters ... --quiet` 完成只读诊断：

- 发布版 EXE 能在真实主机启动、生成报告并正常退出。
- 进程架构为 x64，运行于 .NET 8.0.15。
- Windows Network Connections COM 枚举到 7 个可控制网络连接，无枚举异常。
- `.NET NetworkInterface` 枚举到 8 个接口；额外接口为不可控制的 Loopback，差异符合预期。
- 当前机器已安装 `.NET 8 Windows Desktop Runtime`。
- 发布版 EXE 当前未数字签名。
- 当前 Windows 用户下未找到 `NetRelay AutoStart` 任务，因此现有任务实例无法验收。

诊断报告包含设备识别信息，已在检查后从仓库工作区删除。

## 5. 第五阶段发布验收

- 旧 `artifacts/publish/win-x64/` 已清理，发布目录只保留当前统一发布文件和 `BUILD-INFO.txt`。
- `BUILD-INFO.txt` 记录发布对应 Commit、构建时间、EXE/DLL SHA256、运行时标识和签名状态。
- 发布版使用 `--startup` 启动后保持隐藏且进程响应正常。
- 第二次使用 `--startup` 启动后立即退出，后台仍只有一个 NetRelay 实例。
- 管理员运行上下文中成功注册 `NetRelay.App` AppUserModelId 与 `netrelay://` 协议。
- 验收结束后已结束测试进程，无 NetRelay 进程残留。
- Windows 通知中心真实按钮点击、通过界面正常退出和托盘菜单退出仍需用户手动确认。

## 6. 待受控实机验证

下列测试可能短暂断网或改变系统状态，当前未执行：

| 测试 | 操作 | 通过标准 |
| --- | --- | --- |
| 热点切换 | 有线网与手机热点同时连接后禁用有线网 | 热点保持联网，禁用后复检成功 |
| 自动回滚 | 模拟备用网络在禁用后失效 | 目标网卡立即重新启用并记录回滚 |
| 每日/每周规则与提前通知 | 创建数分钟后的 Daily/Weekly 测试规则 | 提前通知、执行、日志与恢复符合配置 |
| 休眠恢复 | 跨越触发时间休眠后唤醒 | 两分钟容差内补偿执行，超出容差记录过期 |
| 开机自启 | 启用任务后注销并重新登录 | 程序无窗口闪烁地进入托盘，任务指向当前 EXE |
| Windows 10/11 | 分别在目标系统运行 | 原生控制、通知、DPI 与托盘行为符合预期 |
| Windows Rich Toast 点击 | 在发布版提前通知中点击延迟与取消 | 权限、延迟值和后台隐藏行为符合规则配置 |
| 正常退出 | 分别通过关闭对话框与托盘菜单退出 | 一秒内无 NetRelay 进程残留 |

## 7. 发布边界

- 当前 Windows 客户端发布为 `win-x64` 自包含模式，目标电脑无需预装 .NET 8 Desktop Runtime。
- 第八阶段已生成 Inno Setup 安装器候选和统一更新 ZIP；安装器尚未完成干净 Windows 10/11 虚拟机验收，程序和安装器仍未代码签名，也尚无 SmartScreen 信任积累。
- 自动恢复任务保存在进程内存中，程序完全退出后不会继续执行。
- 网络变化规则与时间规则仅在 NetRelay 托盘进程运行时有效。

完成第 6 节剩余的受控实机测试后，本轮完整验收即可结束。

## 8. 第六阶段（B6）正式部署与综合验收

- **编译与发布验证**：
  - 服务端 `NetRelay.Server` 成功以 `Release` 配置编译，解决并清除了 1 项 CS8602 空指针潜在警告，实现 0 错误 0 警告交付。
  - Windows x64 客户端成功进行 `dotnet publish -c Release -r win-x64 --self-contained false` 框架依赖打包，生成于 `artifacts/publish/win-x64/`。
  - 生成的 `BUILD-INFO.txt` 记录了发布时的 Commit `e0b18ec998fb35737c2635c488078750048e3cc4`、构建时间及关键组件哈希。
- **运维脚本加固与静态审查**：
  - 对 `deploy/scripts/` 下的 `deploy.sh`、`migrate.sh`、`backup.sh` 和 `restore.sh` 进行了全面健壮性升级，增加了 `.env` 配置文件自检、Docker 命令可用性自检、MySQL 容器运行状态和健康检测，以及备份数据完整性 (SHA256) 验证。
- **测试通过状态**：
  - 43 项客户端回归测试和 25 项服务端集成测试在 Release 模式下完全通过。

## 9. 维护阶段与缺陷修复及 B7 阶段规划（2026-06-14）

- **已完成的缺陷修复与维护**：
  * **公告已阅保存失效与重复弹出修复**：修复了 WPF 客户端多个配置服务实例竞态覆盖 config.json 磁盘文件的 Bug，修改为共享单例，且在公告关闭时立即存盘。
  * **策略类型 400 报错与 UI 简化**：去除了后端和前端中无功能支持的隔离、暂停、限制通信三个动作类型；彻底移除了前端下拉选择框，改为依据有无版本限制动态自动判定 `global` 与 `version_range` 策略类型提交。
  * **全状态与等级徽章汉化**：对管理前端页面中所有的 Status、Severity 等列表单词进行了全局汉化转换，消除英文直观展示障碍。
- **第七阶段（B7）反馈强化与设备库设计规划**：
  * **反馈指纹集成**：扩展客户端反馈表单，跟随反馈文本共同收集与上报本机的 Device ID Hash 与 Installation ID。
  * **详情 Modal 与一键封锁**：隐藏表格内臃肿的正文，替换为弹窗查看，并在弹窗内提供“一键封禁设备”快捷填充通道。
  * **注册设备库与心跳度量**：添加指纹名册表，显示最后活跃时间，并在前端判定近 48 小时内有通讯的为 `“近期活跃”`，其余为 `“长期沉寂”`。

## 10. B7 接手验收状态（2026-06-15）

- **实现范围**：
  * 服务端反馈记录新增 `device_id_hash`、`installation_id`、`client_version` 与 `os_version`，并新增 `/api/v1/feedback/my` 和 `/api/v1/admin/devices`。
  * WPF 客户端反馈窗口新增历史页签和环境信息上报，新增 `ModernMessageBox` 替换原生消息框。
  * 管理后台反馈详情展示设备与环境上下文，设备库、反馈、公告、更新包和限制策略列表新增搜索、筛选和前端分页。
  * 管理后台反馈附件下载改为 `fetch` Blob 流，避免二次认证过期时退化为 `attachment.json` 下载。
- **自动验收**：
  * `dotnet build NetRelay.sln -c Release --no-restore` 通过，0 错误；当前测试项目输出 32 个 nullable/重复 using 警告，需后续清理。
  * `dotnet run --project server/NetRelay.Server.Tests/NetRelay.Server.Tests.csproj -c Release --no-build --no-restore` 通过，25 项。
  * `dotnet run --project tests/NetRelay.RegressionTests/NetRelay.RegressionTests.csproj -c Release --no-restore` 通过，43 项。
  * `npm.cmd run build` 通过。
- **接手时发现并修复的问题**：
  * 修复管理后台 JSX 结构错误导致的构建失败。
  * 移除服务端 503 错误响应中对外暴露的异常堆栈详情。
  * 将当前环境下不稳定的 `HttpListener` 回归夹具改为本地 TCP 503 响应夹具。
  * 恢复常驻服务 `NetRelay:AutoMigrate` 默认关闭并受配置控制，避免生产启动时无条件迁移数据库。
- **最终验收结论**：
  * 维护者于 2026-06-15 确认管理后台筛选、分页、详情弹窗、一键封锁和附件下载二次认证均正常。
  * 维护者确认 WPF 客户端现代弹窗、反馈历史切换和历史详情展示均正常。
  * 维护者确认真实 MySQL Migration、生产部署包生成与宝塔环境升级均正常。
  * B7 阶段验收通过。

## 11. 第八阶段交付打包当前状态（更新于 2026-06-18）

- **状态**：进行中，已完成首批实现与自动验收，尚未达到正式发布验收条件。
- **已实现**：
  * 统一 Windows 客户端交付脚本，可生成完整发布目录、管理后台更新 ZIP 和 Inno Setup 安装器。
  * 客户端与独立更新器使用压缩单文件 `win-x64` 自包含发布；安装器无需检测或下载 .NET Desktop Runtime，并支持标准卸载、自启动任务清理、通知协议注册清理、旧版散文件清理和可选用户数据删除。
  * 客户端与独立更新器使用签名清单 sidecar，独立更新器再次校验签名、包大小和 SHA256，并加固异常回滚。
- **自动验收证据**：
  * `dotnet build NetRelay.sln -c Release --no-restore`：0 警告、0 错误。
  * 客户端回归测试：43 项通过；服务端测试：25 项通过；管理后台生产构建通过。
  * Inno Setup 6.7.3 编译成功，生成 `artifacts/delivery/win-x64/installer/NetRelaySetup.exe`。
  * 单文件自包含开发预览、更新 ZIP 与安装器构建成功；预览目录根部已压缩为 `NetRelay.exe`、`NetRelay.Updater.exe`、`BUILD-INFO.txt` 和 `Assets/`，管理员权限执行 `NetRelay.exe --verify-update` 返回 `0`，独立更新器可正常启动。
  * 首次运行隐私与服务同意弹窗已按当前磨砂玻璃样式修正布局，底部“不同意并退出”和“同意并激活”按钮不应被窗口边界裁切；未勾选协议时主操作按钮保持禁用并显示“请先勾选同意”。
  * 发布阶段版本号已在维护者逐次授权下用于在线更新链路验收；当前最近一次重新打包版本为 `1.2.9`，安装器和程序均未签名。
  * 更新发布管理已支持查看版本详情与更新日志；版本、通道和架构组合一旦创建即不可覆盖，撤回后仍保留记录与审计历史，修复内容必须提升版本号重新发布。
  * 客户端关于页已改为读取统一产品版本常量，不再硬编码显示版本；服务端更新检查按最高语义版本选择已发布记录，不受发布时间先后影响。
  * 客户端关于页新增“使用帮助”入口，内置帮助弹窗已按当前真实能力编写，明确说明只有启用/禁用与安全回滚，没有真正的一键切换网卡。
  * 更新发布草稿新增强制更新标志；客户端启动检查无新版时静默，有新版时展示更新日志确认窗口，手动检查复用该窗口；关于页可查看仅包含已发布版本的更新历史。
  * 客户端新增脱敏诊断日志，覆盖启动、更新检查与下载、反馈提交等关键阶段；反馈附件与手动导出包含诊断日志，手动导出不再包含原始配置文件。
  * 管理后台更新包上传已显示真实传输百分比、已上传大小和总大小；传输完成后单独显示服务端 SHA256 计算与草稿保存阶段，并在上传期间锁定表单、防止重复提交。
  * 服务端更新包下载端点已兼容旧客户端的无协议头 `HEAD` 探测，避免错误回退至不存在的 GitHub Release。
  * 宝塔更新 ZIP 上传上限统一为 `512MB`；更新包默认保存在 `/www/server/netrelay/data/releases/{channel}/{version}/{architecture}.zip`。
  * 包含上传进度功能的管理后台生产构建通过，并已成功生成最新 `artifacts/baota-portable.zip`。
  * 修复更新器启动参数手工拼接导致目标目录结尾反斜杠破坏引号的问题；客户端改用 `ProcessStartInfo.ArgumentList`，新版更新器可兼容恢复旧客户端产生的畸形参数，并改为无控制台窗口运行。
  * 独立更新器新增与客户端浅色磨砂风格一致的进度窗口，展示等待退出、签名与哈希校验、备份、文件写入、自检、回滚和完成结果，避免用户看到无反馈的终端闪退。
  * 已按维护者授权将当前客户端、独立更新器与安装器版本提升并重新打包到 `1.2.9`，对应预览目录、在线更新 ZIP、Windows 安装器和宝塔便携包均已重新生成。
  * 首次运行同意并激活后，启动阶段临时使用显式退出模式，避免隐私同意弹窗关闭时触发“最后窗口关闭即退出”，导致托盘图标闪现后主窗口未打开。
  * 开机自启 `--startup` 路径不再先显示透明主窗口再隐藏，而是只初始化托盘、规则调度和一次性启动更新检查；用户从托盘打开时才首次显示主窗口，避免登录后出现黑屏窗口。
  * 自动化提前提醒不再同时发送托盘气泡与 Windows Toast；现在每次只发送一条保留延迟/取消按钮的短时 Toast，并移除不会自动收起的 `reminder` 场景。
  * 官网下载按钮已固定指向同站点 `downloads/NetRelaySetup.exe`；宝塔打包会自动携带正式交付安装器，Nginx 按附件且禁止缓存方式提供下载，避免官网部署后仍提示下载地址未配置。
- **待验收与阻塞项**：
  * 干净且未安装 .NET Desktop Runtime 的 Windows 10/11 虚拟机中验证正常安装、启动、升级、卸载和用户数据删除行为。
  * 对真实服务端更新下载、签名拒绝、哈希拒绝、自检失败回滚进行完整演练。
  * 已交付的旧 `1.2.0` 主程序与旧更新器同时存在时无法通过更新 ZIP 自举修复参数启动缺陷；受影响测试机需一次性手工替换新版 `NetRelay.Updater.exe` 或使用新版安装器重装后，再验证后续在线更新。
  * 当前服务端仍使用在线操作密钥签名 `update-manifest`，与离线发布密钥安全规范不一致；正式发布前必须迁移。
  * 配置可信代码签名证书并验证 SmartScreen 与高权限安装体验。
  * 首次运行隐私同意弹窗中的“用户协议”与“隐私政策”当前仅作为同意文本展示，不再提供点击跳转，避免打开空页面或无效外部页面；若未来重新启用可点击协议详情，必须先提供真实有效的协议页面。
- **当前 Windows 主机实机验收**：
  * 早期框架依赖安装器曾通过 Runtime 注册表检测实测；当前已切换为自包含发布，安装器不再执行 Runtime 检测或下载。
  * 安装器成功安装到 `C:\Program Files\NetRelay`，主程序、独立更新器、开始菜单快捷方式和卸载项完整，安装版 `--verify-update` 自检返回 `0`。
  * 安装版真实启动正常，并按当前用户注册 `NetRelay.App` 与 `netrelay://`，注册命令正确指向安装目录。
  * 正常卸载显示“是否同时删除用户数据”必选弹窗；维护者选择“否”后，程序文件、开始菜单快捷方式、卸载项、通知身份、协议注册和自启动任务清理完成，配置与日志完整保留。
  * 静默卸载不显示交互弹窗并默认保留用户数据，避免无人值守流程卡死或意外删除用户数据。
  * 简体中文语言包已纳入仓库，安装器向导使用中文并始终显示安装路径选择页。
  * 安装器“开机自启动”和“桌面快捷方式”选项已分别完成勾选与取消勾选实测：勾选时正确创建并指向自定义安装路径，取消时均不创建。
  * 安装器始终使用管理员安装模式；客户端因网卡控制能力强制请求管理员权限，不提供会使核心功能失效的普通权限安装选项。
